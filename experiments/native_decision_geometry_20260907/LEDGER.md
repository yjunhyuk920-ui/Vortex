# Scoped ledger, unchanged mission

O1 OPEN: constructor accepts only declared scalar affine head, not arbitrary checkpoint.
O2 OPEN: source -> native argmax -> unchanged scalar continuation demonstrated; no HF body.
O3 LOCAL PROVED: exact product domain + monotone double rounding + earliest tie descent;
chosen raw score signed zero preserved; full stochastic/logit/KV contract OPEN.
O4 PARTIAL: serialization/logical probes/algorithmic complexity explicit, no target upper bound.
O5 OPEN: head-only whole-model scope rejects promotion even with zero head cost.
O6 LOCAL: C/NumPy/Fraction checks, deterministic sources/traces/hash regeneration.

Decision D-DG: preserve numeric index as auxiliary; no core admission or feasibility uplift.
Failure F-DG-1: exact real argmax ignores native rounding ties.
Failure F-DG-2: lower query field bytes alone hides 13x source and startup costs.
Failure F-DG-3: greedy maximum cannot replace same-seed sampling or full-logit API.
Failure F-DG-4: current output equivalence is not continuation equivalence.
Assumptions: finite BF16; coefficient[-1,1] multiples2^-16; separate FP32 operations;
scalar hidden provided exactly; source memory/host preprocessing not free.
Architecture: immutable interval-envelope source; paid loader; current scalar address;
monotone native max descent; one original leaf evaluation; unchanged continuation.

Next: find/construct a cheap NONIDENTITY context-dependent source through the wide body
and its native rounding. Do not treat high-dimensional support queries, CDF masses,
or future-state decoding as oracles. Do not just enlarge this scalar test or compile
all responses. Global mission/O1-O6/latency/VRAM success remains OPEN.
