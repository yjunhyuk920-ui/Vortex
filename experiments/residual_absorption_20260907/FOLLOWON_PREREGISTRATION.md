# Non-enumerative history quotient, derived before first experiment run
The conditional whole-branch theorem has an additional constructive consequence: if every legal token embedding is certified for every layer and any history length<=Tmax, residual streams equal the current original embedding at every layer. K/V and final logits become token-indexed. This is not assumed from token determinism (prior causal_cut failed there); it follows from certified erasure of all context-dependent residual writes. Construct token/layer KV tables and exact logit rows with the original numeric kernels; at query time append the actual original KV values, return original logits, preserve the same RNG consumption. Table runtime must contain NO matrices and MUST reproduce complete KV.

This resolves a previously missing causal link on certified models without enumerating V^T histories. It still enumerates V tokens, stores L*V*2k + V^2 + Vh BF16 entries, and pays V*(2Lhk+Vh) MAC for table creation, plus metadata/certificate scans. This precompute/space cost is not free and is not mission acceptance. Public-model applicability remains unknown.

Freeze Tmax=32, same checkpoint seeds/scales as initial preregistration. Only compile a table executor if global proofs succeed for all tokens/all layers including finite attention-score preconditions at Tmax. No numerical sampling as proof of the universal history statement. Save every certificate bound/margin decision and count all construction accesses. Refuse >Tmax at runtime (outside this bounded theorem; no claim to unrestricted mission). For unqualified checkpoints do not construct token tables or call them compressed. Compare both guarded and table runtimes to independent native state/RNG sequences. Preserve negative certificate decisions.
