# Scoped research ledger — residual_absorption_20260907

Unchanged mission, CTC-2026-09-05; base PR138/91904a0. Existing failed families remain scoped.
THEORY_STATUS=NOT_ESTABLISHED; HARDWARE_STATUS=NOT_TESTED; CORE_ADMISSION=false.
O1-O6 full mission OPEN; THREE_QUALIFYING_NEW_PRINCIPLES=false.

DECISION: Keep conditional residual-fiber / live-KV theorem and its constructive global
history quotient as auxiliary evidence. No universal engine promotion, no measured GPU speed.
ASSUMPTIONS: immutable original snapshots; finite sound projection envelopes; declared
FP32/RNE-BF16 ABI; finite BF16 SiLU table range; stable exp range; no dropout/no RoPE;
exposed roots logits and KV plus unchanged sampler; context<=32 for the global theorem.
Costs of startup validation, original scans, tables, NumPy FP32 containers and ordinary
nodes remain charged. No matrix object in TableExecutor. No target future token given.

FAILURE: ordinary-scale controls have no residual identities; all source work remains.
At scale16 reference identities exist but this loose proof rejects. These are different failures.
Even the favorable table has cold overhead and target-scale tables≈96.20GiB; no target closure.
Do not call this a lower bound for other representations or a proof the mission is impossible.

ARCHITECTURE: compiler exact L1 envelopes -> proof-before-branch-read -> mandatory K/V writes;
then, only after global token/layer certificate, lookup exact K/V+logit rows and append real KV.
The quotient is derived from erasure, not assumed from determinism or fitted on samples.

NEXT: a cheap nonidentity context-dependent transition for ordinary inputs is missing.
Do not extend the project by raising embeddings, fitting hidden changes, larger truth tables,
increasing solver limits, or benchmarking a permanently context-blind favorable model as 405B.
No broader exact-dead-code family is rejected. Need a new source/transfer or decisive public fact.

VALIDATION:19 tests,455 replayed run files+2 cost/witness files; actual raw KV agreement;
independent Fraction/C endpoint/projection checks. No full HF/GPU/4BQ4/TTFT/Actions run.
README_CURRENT=true: current artifact and frontier are linked, historical results unchanged.
