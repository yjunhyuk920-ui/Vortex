# Symbolic source construction / 2026-09-07
Base: yjunhyuk920-ui/Vortex PR137 de11732ec478773164aeb2cf7de02efd0483c9e9.
Mission and CTC-2026-09-05 unchanged; no admitted core candidate.

Intended full theorem remains an automatic exact causal executor for every mission
checkpoint/input/RNG, peak<=8GiB and same-machine 4B Q4 p50<=1.2/p95<=1.5 with all
construction/storage/movement charged. This study closes no full O1-O6 obligation.
A bounded source-construction pilot targets the missing non-enumerative native proof
bridge, NOT a substitute for the primary theorem. No GPU or public-model claims.

Three compared source schedules (not three qualifying new discoveries):
A. Generate bounded short native expressions from source constants and discharge a
   universal finite-word miter; source AST replaces exhaustive response enumeration.
   Credible whole-work >=10x route NOT ESTABLISHED: finding sufficiently small code
   and bounding proof search remain essential. This is a bounded LambdaForge/source
   construction continuation, not renamed evidence of a new engine.
B. Infer response fragments from selected probes plus exception list: observations
   alone cannot certify unseen inputs; an exact source proof is still required.
   No table enlargement or rank samples are run. No core backend.
C. Interpret source constraints per query and stop when outputs/state are forced:
   finite algorithm exists, but no small paid query service bound; source constraints
   and solving are not free. No core backend.

A uses an explicitly finite candidate grammar, FP32 RNE operations and BF16 RNE
stores. Symbolic input words range over ALL finite BF16 values (including +/-0 and
subnormals), not a sampled alphabet. NaN output is canonicalized to 0x7fc0 by the
DECLARED reference ABI; this is not a PyTorch/CUDA payload promise. No FTZ/DAZ.
Source/candidate cases fixed before runs:
- shared operand products 3/8 and 1/8 with FP32 intermediates;
- same expression but each product stored BF16;
- two-input dot 3/8*x + 1/8*y, FMA candidate;
- overflow cancellation 2*x + (-2)*x, zero candidate;
- zero-times-input, constant positive zero candidate;
- current output x-x plus successor state x, attempt to erase both;
- same state case retaining x is allowed.

Candidates: generated zero, x, scaled x using source constants/their exact sums,
and FMA for a two-product FP32 sum, plus unchanged source. No superinstruction may
hide the original source. Quantify output AND all declared successor registers.
Counterexamples are replayed and used to discard later candidates. No bounded
examples establish equivalence. Only solver UNSAT for the miter establishes equality
in the declared model. SAT/UNKNOWN/TIMEOUT is never promoted. Fixed timeout 8s/query,
fixed rlimit 2000000, seed 7, max 16 non-baseline candidates/case. Baseline retention
is compile-time non-optimization, not a successful acceleration or runtime fallback.

Independent C scalar replay on 4096 deterministic finite words/pairs plus any solver
witness. Universal SAT proof replay is same solver, not independently checked proof
certificate. Retain SMT scripts, solver version, all statuses/witnesses, source /
selected instruction counts and source/selected literal-byte accounting. Hardware
latency, model-wide traffic, peak VRAM, full Transformer/RNG remain NOT TESTED.
