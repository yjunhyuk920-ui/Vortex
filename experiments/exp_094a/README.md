# EXP-094A

Official public-checkpoint Parareal deep-residual transport Gate.

```bash
python -m pip install --disable-pip-version-check -r requirements/fixed-public-dynamic-executor.txt
python -m pytest -q tests/exp_094a
python experiments/exp_094a/run_experiment.py \
  --config experiments/exp_094a/config.json \
  --output-dir results/exp_094a/local
```

The candidate is generated from `F(g) + G(c) - G(g)` and must complete before the delayed official target continuation begins. A positive candidate-source signal still does not authorize token commitment: a sound certificate, physical kernels, complete state handling, and target-hardware evidence remain separate Gates.
