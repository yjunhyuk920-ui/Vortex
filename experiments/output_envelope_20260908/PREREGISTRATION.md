# Native output-envelope public-weight screen — 2026-09-08

Base PR146: 66c01825635a357f86f9dfa5369088d7a239c92d. Fixed mission and CTC unchanged.
This is bounded discovery/measurement, not an admitted universal core.
The other checkout's untracked correlation_source experiment is untouched.

## Final theorem and obligations
For every public unmodified dense405B checkpoint and every legal prefix/input/RNG,
construct a finite causal executor preserving original output and required next
state, <=8GiB GPU, same-machine native4BQ4 p50<=1.2x,p95<=1.5x, original TTFT,
with every preparation/storage/movement/arithmetic/verification cost charged.
Whole-mission O1-O6 remain OPEN. Current local exactness is not HF/CUDA identity.

## Three compared flows, before any public weight payload is examined
A. Output-cohort certification: different coefficient rows can share an observable
BF16 result. Build per-coordinate extrema per packet, propagate both extrema in
the original FP32 reduction tree, and broadcast only a safe nonzero finite BF16
singleton. Otherwise evaluate the original rows once. No candidate answer oracle.
For uniform packet b and certified row fraction f, work/payload ratio is
2/b+1-f. Thus10x requires f>=0.9+2/b before other costs. Heterogeneous actual
outputs are a decisive counterexample for this broadcast format. Selected bounded
screen. Interval/cohort tools are known; no unprecedented-method claim.
B. Output contrasts rather than shared values: one scalar plus a compact native
correction could handle heterogeneity, but a cheap correction producer is missing.
Dense residual/Gram/table procedures retain previous rejected costs; not repeated.
C. Delayed causal packet actions: avoid emitting a vector until consumed, but a
cheap native continuation and bounded cumulative forcing debt are missing. Prior
deferred-state PR145 already retained127/128body work; no parameter sweep repeated.
These are distinct flows, NOT three newly qualifying original principles.

## Frozen public screen
HuggingFaceTB/SmolLM2-135M @93efa2f097d58c2a74874c7e644dbc9b0cee75a2.
Only metadata checked: public,134515008BF16 parameters. Layers0,15,29; complete
self_attn.q_proj,mlp.gate_proj,mlp.up_proj,mlp.down_proj matrices. No dtype changes.
HTTP Range must return206 and matching Content-Range/length. Save tensor hashes.
Packet256, contiguous original rows, no search/reordering/tuning.
8 synthetic input vectors per tensor: NumPy PCG64 seed260908, normal, BF16 RNE.
These are synthetic legal activations on real weights, NOT HF-forward traces.
No checkpoint weights committed. Cache originals locally and pin reacquisition.

## ABI, evidence and decisions
Finite BF16 W,x; separate FP32RNE products; +0padding to next power2; fixed balanced
FP32 additions; final BF16RNE; canonical positive NaN output. Any nonfinite bound
intermediate, zero singleton, or unequal BF16 endpoints forces direct rows.
Full output is returned; no RNG consumed, but no full Transformer substitution.
All public outputs compared to direct NumPy. First3rows of first input of every
matrix checked with independent Fraction RNE. Planted dominant-output control
kept separate. Record every packet, candidate/reference bits and actual raw reads.
Compute an explicitly favorable oracle: packet's actual output words all equal.
This is used only to bound this broadcast format, never by the candidate.
If public coverage misses cost gate, stop as core; no packet/seed/tensor sweep or
GPU backend. Count summaries, original retained weights, constructor, input/output,
fallback/direct work. Logical counts are not GPU traffic or latency.

## Persistence and requested session tool
Separate worktree/branch, no main/force/merge/Actions dispatch. Preserve sources,
raw outputs, hashes, logs, costs, exact scope, O-ledger and next decisive gap.
Call user-requested Chat_On_Steroids_Core.session_finish while independent final
verification/handoff remains, follow returned in-scope instructions and call again
when HELD. Do not claim an exact five-minute interval without evidence.
