# Next return point

Read REPORT.md, VALIDATION.json, obligations.json and root NEXT_EXPERIMENT.md.
Canonical HF result is hf_results_v5/summary.json. This round constructed and
executed a restricted native producer extension; it did not close the mission.
The fixed model/input/continuation contract and O1-O6 remain unchanged.

Do not rerun the same sparse fixture as core research. The remaining core needs
a native exact global arbitrary-dense producer or exact dynamic summary/update
that is cheap on all-coordinate changes. Compare three materially distinct
finite candidate mechanisms before selecting one. Reuse the scalar signed-zero
lemma only inside its proven admission domain. Do not infer arbitrary native
injectivity from matrix rank, hide work in transformed gates, or assume global
advice can be split independently per matrix.

Hardware is unavailable by user report, not scientifically ruled out. Continue
available theory in the active persistent goal. Original checkout is not to be
overwritten; preserve concurrent research on the branch. Only primary commits,
pushes normally with [skip ci], and verifies the remote SHA and raw bytes.
