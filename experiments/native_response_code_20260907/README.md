# Finite native response code — 2026-09-07

[Report, proofs, costs and ledger addendum](REPORT.md).
[Preregistered domains](PREREGISTRATION.md). [Validation](results/validation.json).

Exact whole-SwiGLU output-bit source for small explicit BF16 input domains. Not an
arbitrary-input/HF/405B executor. All mission O1-O6 remain OPEN. Source creation is
exponential; no core promotion or target performance claim. Unknown words reject.

Run `python src/run.py`, then `python -m unittest discover -s tests -v`,
`python src/scope.py`, `python src/verify.py`. Use an empty results/run directory.
Python3.13.5,numpy2.3.5,torch2.10.0+cpu,gcc14.2.0 were used. No downloads needed.

Remote source regenerates the171 evidence files. Full original binaries and expanded
Korean explanation are in the user artifact, not all embedded here. No original
upstream checkpoint weights or secrets are stored. See REPORT.md for exact scope.
