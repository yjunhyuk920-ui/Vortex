# Handoff — causal/global producer frontier

## Canonical conclusion

The round established three scoped facts but did not establish the mission:

1. actual HF causal KV exposure of arbitrary binary dense source information;
2. an exact finite no-dense producer for the restricted basis-column family;
3. a standard-causal sign/delta compiler that reaches many independently chosen
   right factors, including a 32-query small-width native trace and a 4-query
   `25 x 216` area-5400 native trace.

Read `REPORT.md` and `POST_PREREGISTRATION_EXTENSION.md` before continuing.

## Do not overclaim

- The area-5400 32-query native trace has not been executed.
- Global nonlinear advice/direct-sum is not proved.
- The restricted producer depends on binary weights and special basis states.
- 405B/CUDA/8GiB/native4BQ4 p50/p95/TTFT remain NOT TESTED.

## Next constructive frontier

Build an explicit zero-error finite-word arbitrary-matrix producer with globally
paid advice, or a dynamic summary whose update does not collapse to arbitrary
dense column aggregation.  Any candidate must provide a finite encoder,
runtime address rule, decoder, native ordered exactness argument and complete
cost bound.  Boolean-semiring randomized MatVec is not sufficient.

The sign/delta compiler should be used as an adversarial causal input source:
successive legal right factors can differ densely, so simple response-column
updates are no longer an admissible hidden assumption.

## Focused replay

From:

```text
C:\ChatOnSteroids\Vortex-output-envelope\experiments\causal_global_bridge_20260908
```

run:

```powershell
$env:PYTHONPATH=(Resolve-Path '.').Path
& '..\native_global_transition_20260908\.venv\Scripts\python.exe' `
  -m unittest `
  test_causal_kv_exposure `
  test_basis_column_producer `
  test_causal_rank_one_trace -v
```

Do not rerun the old native-global 3510-file replay unless that experiment is
actually modified.

Final local gates: causal-global `14/14 PASS`, related nonlinear/geometry
`28/28 PASS`, and `scripts/run_validation.py` exit `0`. The full tests directory
was attempted but is not a PASS because the current Windows environment lacks
several historical test dependencies; see `VALIDATION.md` rather than rerunning
blindly.
