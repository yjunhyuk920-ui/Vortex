# Handoff — whole-block transport comparison

Parent: `yjunhyuk920-ui/Vortex`, `research/source-search-no-admission-20260905`, `cfb28c0a803ce16e9eaf6fbefab0ad40a8aa0c76` (PR #127). Read root AGENTS and CTC before continuing.

Scientific state: THEORY_STATUS=NOT_ESTABLISHED; CORE_ADMISSION=false; HARDWARE_STATUS=NOT_TESTED; O1-O6=OPEN. These artifacts do not complete the mission. The three candidate constructions also do not meet the mandate's three qualifying-new-principles criterion; preserve that shortfall.

Constructed: (1) exact rational generator for the even quadratic component of a whole bias-free SwiGLU block, (2) explicit serialized/decoded binary-power monomial projection program, (3) native consumer-level attention pullback witness. See the report for the exact algebraic domain, all failure cases and charged costs. No actual HF checkpoint, GPU, complete RNG or successor-state executor was tested.

Do not expand the failed direct quadratic materialization, static monomial program or uncorrected query pullback. A new core needs a material information source and a complete native/cost construction that defeats these failures, not a renamed residual, a free word corrector, or more tiny truth tables. Existing root NEXT_EXPERIMENT remains the primary construction obligation.

Reproduce using README commands. Four deterministic result files were regenerated byte-identically; 12 unit tests passed. Tests check the correctness of the constructors and the rejection evidence, not 405B goal achievement. A scoped randomized sample pass for monomial gate recasting is not native equivalence; the explicit FP32 accumulation witness defeats that extrapolation.

The two preceding local ZIPs were input context only; their contents are not silently included or treated as remote-committed. This directory is the current round. Parent policies and earlier evidence are preserved. README/state/validation/next-obligation root updates are additive.

Remote completion must be verified by reading the created commit/tree, branch ref and changed entry files. This snapshot cannot truthfully contain its own eventual commit SHA; a separate post-commit receipt records it. No force push, direct-main write, unrelated merge or Actions dispatch is authorized by this handoff.

Raw words are also archived as `results/raw.jsonl.gz` for repository persistence. `gzip -dk results/raw.jsonl.gz` restores the byte-identical JSONL, or rerun the frozen generator. The uncompressed SHA is in SHA256SUMS.txt.
