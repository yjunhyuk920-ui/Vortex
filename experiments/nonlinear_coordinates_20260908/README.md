# Nonlinear coordinates — bounded constructive study

[Report](REPORT.md). No full-mission or hardware success claim.

From a checked-out copy of this branch:

```sh
python experiments/nonlinear_coordinates_20260908/restore.py /tmp/vortex-nonlinear --run
```

Python3/NumPy2.3.5/system C compiler required. The four text capsule pieces contain15 verified text files: all final Python/C source, tests, preregistration and addendum, full Korean report, local README/HANDOFF, expected109-file manifest and validation. Restore verifies XZ/source JSON hashes and refuses conflicting overwrites. It then explicitly runs verification only with --run.

The source-only capsule was restored into fresh local directories; C compilation,22tests and109sciencefile hashes matched. No network/model downloads or hosted rerun are needed. Raw scientific files regenerate. User ZIP also preserves raw binaries, development logs, pre-scope-addition manifest/run/scope and pre-step-C source. Those logs/raw files are not all embedded in this Git capsule.

The final scientific manifest is56c016845ebee52901283b15680f7460270f4a3ef9ecb0465a1bde1c4c3b728d. Added scope probes changed an earlier frozen manifest; earlier artifacts remain in the userZIP. Capsule compression is archival storage, not an inference compression result.
