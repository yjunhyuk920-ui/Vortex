# Nonlinear state coordinates and observer transport — 2026-09-08

Base: verified PR143 /9ac4b91041afa0763aa35de53a5f2393343a0d38. Fixed mission and CTC-2026-09-05 unchanged. THEORY_STATUS=NOT_ESTABLISHED; HARDWARE_STATUS=NOT_TESTED; CORE_ADMISSION=false; FULL_MISSION_O1_O6=OPEN; THREE_QUALIFYING_NEW_PRINCIPLES=false. This is a bounded study of SYNTHETIC REVERSIBLE BOOLEAN programs, not BF16 neural networks. Full Korean derivations and complete executable artifacts are in the checksummed source capsule; see README for restoration.

## Constructive result and obligation
We address conditional O1/O2/O3/O4: finite construction, causal same-size nonlinear state representation, original observer and state preservation, and paid preparation. A nonlinear bijection need not reduce rank/state size to eliminate repeated conversion. However no cheap map from a general unmodified native Transformer to this representation has been constructed. No theoretical or empirical mission promotion follows.

## A1. Explicit finite constructor
N=2^d states; m inputs;d<=16. Given only callable original T_u, evaluate the entire T0 orbit from0. Require a single N-cycle. Store D(k)=T0^k(0),E(D(k))=k. For each u infer b_u=E(T_u(D(0))),a_u=E(T_u(D(1)))-b_u modN; require odd a_u and verify E(T_u(D(k)))=a_u k+b_u modN for EVERY k. Fail with a witness otherwise. This is complete only for this T0-normalized affine chart, not for all nonlinear coordinate choices.

Runtime stores z=E(s); reads coefficients and computes z'=(a_u z+b_u)&(N-1), returning D(z'). Thus T_u(D(z))=D(z') and exact original-state/output preservation follows inductively for every accepted model and every input sequence. Python/C read the serialized source; no target future states or hidden chart supplied to this constructor.

Successful construction calls the original transition N+m(N+2) times and additionally pays orbit tables, inference, verification, addressing and storage. Source size24+4m+4N bytes. At d12,m4:20488calls/16424B versus328B original compact circuit and32768B fully expanded transition table. The call count alone is2561times eight original transition calls; not a latency ratio. Explicit tables grow exponentially; a hypothetical32bit/u32 version uses32GiB for E/D alone. This is not a universal lower bound or an implemented32bit executor.

## A2. Direct inverse-paired IR constructor — no state enumeration
Original input IR must ACTUALLY contain T_u=E^-1 o A_u o E, A_u(z)=a_u z+b_u mod2^d. E is a sequence of checked NOT/CNOT/Toffoli involutions with legal distinct controls/targets. The constructor validates primitive semantics, common exact prefix, reversed suffix, supported odd affine middle, and original observer. It never evaluates a state during construction. Explicit symbolic relation, not learned/sample-fit conjugacy.

If ORIGINAL observer O=E, maintain z=E(s) and execute only z'=A_u(z), output z'. Proof: E(T_u(s))=A_u(E(s)); applying the identical original observer yields z'. Arbitrary legal continuation and same-output/RNG input selection preserve the induction. When ORIGINAL observer is identity, actually compute E^-1(z') at each step and charge that decoder. The two observation contracts are distinct reference programs; outputs are not changed to manufacture acceleration. E is bijective, so full state remains recoverable, not a behavioral quotient. Verification-only decoding is not inside the encoded-observer hot path.

At d12 the E circuit has72 reversible gates. Original transition+encoded observer executes216gate applications; transformed encoded-observer runtime eliminates them but retains the SAME12bit multiply/add/mask and all header/coefficient/address/output/code costs. Original-state observer runs144gates versus72decoder gates after transformation, not a10x route. Source328B equals compact original, so no compression claim. Initial encode72gates, constructor720primitive visits plus648tuple-comparison upper bound are paid; source reads2888IR scalar fields in the declared instrumentation. No CPU/GPU timing improvement or target-sized unit-cost bigint model is claimed. The exact inverse pairs and observer were intentionally embedded in positive controls; there is no generic HF detector or native rounding bridge.

## Other candidates and scope countercontrols
B: input-specific cycle charts make each transition +1 but require E_v D_u at symbol switches. Actual8state witness gives5 while omitted-switch code gives1. Explicit switching table isNentries per pair. This does not lower-bound all switching algorithms. Noncommuting affine maps z+1 and3z+1 on64states give4/2 in opposite orders, proving that noncommutativity does not exclude common affine coordinates.

C: around BF16 1.5,65537FP32predecessors from0x3fbf8000 through0x3fc08000 share that RNE output;65539probes including outside endpoints agree between independent integer and C calculations. Output-only inversion needs17fixed side bits for that fiber. NOT per-rounding/per-token mandatory history and NOT global Transformer noninjectivity. A bijective conjugacy cannot turn a noninjective transition into an odd affine permutation, but a cheap noninjective target remains possible. Explicit64state nonlinear E with T=E^-1 floor(E(s)/2) verifies every state and has32image states. Additional12numeric edge probes preserve declared signed-zero/subnormal/infinity/canonical-NaN behavior. This local primitive audit is separate from the Boolean recurrence and supplies no native neural lifting theorem.

## Registered experiment and reproducibility
Widths6,8,10,12;seeds17,29;four symbols;E length6d gates;nonlinear XOR witnesses saved. Finite24attempts:8accept,16refused swap/collapse perturbations.43520finite and87040two-observer complete state/symbol comparisons agree with independent C and Python original/serialized executors.6144causal steps use external input and independently own-output/LCG-selected input; original decoded state, declared output, RNG and chosen input agree.8history-order witnesses retained. At d12 seed17 histories[0,1,2]/[1,0,2] give original states469/1176 and encoded outputs23/13; not context-independent output.

22tests and109scientific files regenerate to final frozen manifest56c016845ebee52901283b15680f7460270f4a3ef9ecb0465a1bde1c4c3b728d. C recompiled; source-capsule fresh restoration verified. Final manifest includes later explicit edge/noninjective scope additions; prior manifest/run/scope and earlier C sources/logs remain in userZIP. Do not assert every file is unchanged since initial development. Full repository suite/Actions/HF/fullKV/RNG/CUDA/405B/8GiB/native4BQ4/TTFT/latency NOT RUN. Original checkpoint download DNS failure was an infrastructure fact, not scientific evidence.

## Remaining construction
An observed-in-original-checkpoint, cheap native wide-body producer and transported observer are still missing. Do not expand state tables or repeat inverse-paired positive controls as mission progress. Do not assume E^-1AE already solves the encoder. No general impossibility claim.

Existing background, not novelty claims: Haller/Kaszas, Data-Driven Linearization, https://arxiv.org/html/2407.08177v1 (local continuous dynamics, not this exact finite-word model); reversible circuit survey https://arxiv.org/html/1110.2574v2; NVIDIA finite-word arithmetic https://docs.nvidia.com/cuda/floating-point/index.html .
