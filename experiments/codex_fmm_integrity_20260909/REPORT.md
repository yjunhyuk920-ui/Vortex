# EXP-100A integrity repair and actual full-gate replay

## Final authority after independent-review hardening

Canonical result: [hardened_run/result.json](hardened_run/result.json),
SHA256 375a4f4a9d5055429664f0c042c02f2d128fbf2cd8196b1fcdc6ffa87980a121; verification: hardening_verification.json.
The first corrected_run and its review remain historical intermediate evidence.

Independent review found that mutually consistent undercounts could pass the
first classifier. Four actual red mutations are preserved in post_review_red.log.
The final classifier independently derives expected evaluation counts from
retained states, cuts and frozen word widths, and checks exact completed depths
and termination. Raw oracle evaluations are counted before retention.
All six new unittest tests and eleven old functions passed. The FULL gate was
executed again: 84,712 direct and 15,608 oracle evaluations exactly reconcile
across all 50 cells; 132 integer controls have zero mismatches. All old scientific
fields and catalog/control/selected raw bytes match red and first corrected runs.
Cost formulas, selected plans and the bounded negative do not change.

Final source hash:68d8276323cf96f86ef22de661644d318f30dc9da37bd5e2a55bfa8a180fc56d.
hardened_sources preserves all manifest-bound execution bytes. The earlier
source_transport.json refers only to the first corrected intermediate source.
Current full execution used the actual final LF runner. No native/GPU claim.


The repaired CPU run gives a **bounded retained-search rejection**:
REJECT_CATALOGUED_SMALL_COEFFICIENT_RECTANGULAR_FMM_AS_10X_CORE.
It closes the old false invalidity of this registered run, not the VORTEX mission
or the requested construction of three new execution principles.

First-correction authority: [corrected_run/result.json](corrected_run/result.json), SHA256
ee810b7edb2c7e907d05dbb48359e99b587bd8b177e6c578f72c59854d22fd9e. [verification.json](verification.json) binds the red/green comparison
and an independent integer workspace reconstruction.

## Frozen constructive object

The existing finite program enumerates integral catalog factorizations, orients
factor spaces, composes bounded recursion, selects a static-transform cut/word
width, and jointly selects retained family plans under registered I/O bins.
It counts leaf products, factor additions/scales/moves, repair rows, static forms
and favorable workspace. Integer tensor controls prove those finite algebraic
identities, not native rounded HF equivalence.

The actual full run inspects93 keys, admits59 factorizations, retains128 of354
orientations and executes132 integer controls with zero mismatches. Ten block
lengths and five unique shapes give50 cells, representing883 projections in
eight registered families. Beam, per-split, structural, family and I/O-bin
truncations remain; this is not exhaustive over the catalog.

No config, catalog, cost formula, threshold or grant changed. Perfect future
activations, N/A=1, a perfect native repair selector, free offline transforms,
favorable transformed words and1.5x compression are still explicit grants.
This is not a complete upper bound for an actual executable native runtime.

## Actual red failure and minimal repair

The original Windows import failed because resource is unavailable.
portable_only_runner.py preserves only portability changes and the original
scientific classifier. Its full red_run reproduced exactly one integrity fault:
empty_direct_shape_search. original_runner.py preserves untouched Git source.

The old classifier treated every empty direct frontier as corruption.
For K16384,rows128256,columns16384, the actual completed retained search evaluated
2,304 direct plans; all2,304 exceeded workspace. Evaluated depths1..4 ended in
normal beam exhaustion. No permitted direct plan was lost.

New accounting records generated/admissible/rejected counts BEFORE pruning,
minimum-workspace witness, completed depths and termination. Counts must
partition and match the retained frontier; the expected50-key set is checked.
Positive retention caps are validated. Missing/incomplete work or a lost
feasible frontier remains INVALID. Zero-generated and empty-oracle cases remain
conservatively INVALID; no broader structurally-empty configuration claim.
Joint no-plan labels distinguish absent families from the frozen I/O-bin DP.

An actual focused regression failed before classification repair. Then five
new unittest tests and eleven existing plain test functions passed. The complete
frozen scientific gate was executed again. The initial import/focused-red
terminal outputs were observed but standalone transcript files were not saved.
The complete red source/result and corrected run.log are retained.

## Result and independent calculation

| Selected population plan | Block K | Arithmetic ratio | I/O fraction | Favorable workspace bytes |
|---|---:|---:|---:|---:|
| Explicit transforms |8192|0.3825164968636731|0.011845085910137866|6,572,965,180|
| Free-transform oracle |4096|0.13010262621990515|0.00016276041666666666|1,185,070,118|

Both miss the first0.1 gate and final p50 fraction0.011851851851851851.
The best direct plan's compiled forms total117,532,135,092,756 bytes: a large
cold sidecar, not the canonical source plus8GiB pool. Causal blocks and native
repair are not constructed.

The empty cell's minimum is13,145,334,184 bytes=12.2425464764GiB, exceeding8GiB
by4,555,399,592 bytes. Witness: two 8,11,12:p021 steps, then6,8,10:p102,
full cut depth3,2-byte static words. Independent component sums without calling
evaluate_sequence are: activation536,870,912; output4,202,692,608;
left form2,432; local weight4,028; local output8,405,647,360;
metadata116,844 bytes. These sum exactly to the recorded minimum.
This minimum covers generated retained-beam plans only, not all implementations
or actual physical GPU allocation.

## Evidence integrity

verify_repair.py confirms exact equality of every OLD scientific JSON field
between current red and corrected runs, excluding only enumerated new accounting/
selection/scope fields, classification, derived content hash and elapsed time.
Catalog/control/selected-plan raw tables are byte-identical. Config and cost
module raw hashes equal preregistration. The historical hosted INVALID result
is immutable. Its last-bit I/O value differs from the current environment;
exact equality is claimed only for the current red/green pair.

Missing Windows RSS is null, never zero. CRLF transport normalization is accepted
only when the normalized payload has exactly the pre-pinned immutable Git blob.
Actual working-source hashes and raw snapshots are retained; source_commit is
the pre-edit base8affea0, not a clean-source execution claim.

## Remaining mission

THEORY_STATUS=NOT_ESTABLISHED, HARDWARE_STATUS=NOT_TESTED, CORE_ADMISSION=false.
O1-O5 OPEN/O6 PARTIAL. No native logits/RNG/next-KV theorem, causal future producer,
finite-word transformed representation, complete preprocessing/recovery bound,
405B/CUDA/8GiB/native4BQ4/TTFT measurement exists here. User has no405B hardware.
No per-matrix division of global nonlinear advice is used. No general FMM or
native impossibility follows. The next core is a materially different finite
global compiler/address/read/native decoder, not a same-catalog rerun.

INDEPENDENT_REVIEW.md is Sol/high requested static review of the old fault.
Primary verified original source and numerical evidence. Luna/low extraction
supplied exclusions without judgment. Actual selected model metadata was not
exposed; only requested settings are recorded. No recursive delegation/GPU/
agent Git. Agreement does not close a proof obligation.

Tracked runner line endings were normalized after execution, with exact raw
source snapshots retained. source_transport.json confirms identical Python AST;
verify_repair.py accepts only exact raw or CRLF-normalized source identity.
