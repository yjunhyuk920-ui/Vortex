# EXP-100A integrity restoration
Base 8affea0634005600df0c77f8997e0457b43a23d7. Preserve frozen original config,
catalog commit/blob, dimensions, orientation cap, beam, depth, cuts, word widths,
repair fractions, thresholds and all favorable grants. This is a repair of a
still-invalid registered core Gate, not a new three-principle round.

First run with Windows-only import/transport/output portability fixes, preserving
the original scientific classifier. Capture whether empty_direct_shape_search
recurs. Then instrument generated plans, workspace rejection, retained frontier,
beam termination and minima without changing any plan cost/choice. Change the
integrity decision only when a generated, completely evaluated retained search
has accounted every absent direct plan as an explicit budget rejection. Genuine
missing evaluation/corruption/control failures remain INVALID.

Run the complete frozen catalog/control/50-shape/10-block gate after repair.
Compare selected plans and scientific numerical rows to the red run; tolerate
only declared new accounting/status fields and environment/timing/provenance.
A bounded beam no-pass is no general lower bound or full catalog impossibility.
Keep the original invalid result immutable. No sweep or threshold tuning.

The true mission remains arbitrary public unmodified HF405B, batch1, GPUtotal
8GiB, native logits/output/RNG and successor KV on all legal continuations,
same-machine native4BQ4 p50/p95/TTFT. Every construction, source/program storage,
read/transfer, arithmetic, state, verification, recovery and fallback is paid.
This historical gate grants future blocks and repair selectors; therefore it
cannot establish the mission even if a row passes. No native/GPU/405B run.
No per-matrix decomposition of global nonlinear advice. User has no target
hardware. O1-O5 OPEN/O6 PARTIAL; theory NOT_ESTABLISHED, hardware NOT_TESTED.
