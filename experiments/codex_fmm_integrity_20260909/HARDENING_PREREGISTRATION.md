# Post-review integrity hardening

POST_REPAIR_REVIEW identified a self-consistent paired undercount, partial depth,
unknown termination and partial-oracle gap. All four mutations actually failed
the new rejection expectation; post_review_red.log preserves this red run.
The first corrected artifact's 84,712 direct evaluations independently reconcile;
its scoped negative remains evidence, but the generic classifier needs guards.

Derive expected direct evaluations from retained state populations, all cuts and
the frozen word widths; count raw oracle evaluations before retention. Check
exact completed-depth list and terminal condition against the supplied search
contract. Keep every search/cost/selection parameter unchanged. Rerun all focused
tests and the full gate to a new hardened_run directory, then compare unchanged
scientific fields and raw selected plans. Preserve red and first corrected data.
No new native/global/physical-cost claim, no GPU, no three-principle completion.
