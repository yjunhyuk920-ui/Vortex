# Native global transition handoff

Based on actually observed output-envelope commit
`35e0a2eb1d9d64b20d17bd6e0c4afba07d3d70d3`; separate correlation_source PR147
`378fcd584330d17f9d144f64ccbc01721903ea6a` is not merged or modified.
Active worktree C:\ChatOnSteroids\Vortex-output-envelope, branch
`research/native-global-transition-20260908`.

Read REPORT.md, obligations.json, PREREGISTRATION.md, IMPLEMENTATION_LOG.md,
PROTOCOL_CLARIFICATION.md and original mission/CTC first. A/B/C are implemented
on a real pinned BF16 HF model, but each retains all dominant native matrix work.
No qualifying10x core; no target achievement. O1-O6 remain OPEN.

Reproduce inside this directory, with Python3.12 and the pinned environment:

    .venv/Scripts/python.exe acquire.py
    .venv/Scripts/python.exe verify.py --replay results/replay_NEW

Destination must not exist; frozen hashes must never be overwritten. Downloaded
weights and venv remain ignored. Initial failed capture/source and automatic-mask
run remain intact; run_v3 is explicit-mask same-population reference, 3510science
files reproduce after excluding only three volatile timing fields. Original raw
timings remain. Actual 14tests passed; full repository suite/targetGPU NOT TESTED.

User's session_finish instructions are active: call again before final; while
HELD follow the queued instructions and do not send a final response. Latest
instruction requested explicit-mask clarification and a genuinely different next
constructor, not graph/CSE/codec/TOP micro-optimization. The mask clarification
has been executed and all original/candidate logits/KV/RNG/layout and original
v2/v3 outputs agree. After recording current candidates, compare three genuinely
different finite producers with paid>=10x routes, then construct the strongest
missing step. Do not present unavailable new primitives as completed.

Post-commit SHA/remote verification must be recorded in a later receipt/PR; this
snapshot cannot contain its own commit hash. Never claim persistence as theory
completion. No main/force push, unrelated merge or Actions dispatch.
