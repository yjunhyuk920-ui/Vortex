#!/usr/bin/env bash
set -euo pipefail
echo "EXP-079A does not authorize a GPU/kernel or target-server continuation." >&2
echo "Run only the pinned CPU favorable-oracle Gate after its source commit." >&2
exit 2
