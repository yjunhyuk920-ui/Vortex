# Exact continuation point

Read [REPORT](REPORT.md), both preregistrations, source, result hashes and
[independent review](INDEPENDENT_REVIEW.md).

This independent audit adds three scoped facts to the concurrent `dab5774`
Principle III preregistration:

- Independent field gauges A,B,C preserving raw Hadamard are common-permutation
  monomial triples, not arbitrary dense bases; a finite constructor returns a
  certificate or an explicit basis-pair counterexample.
- Native column permutations must carry original leaf/reduction schedules.
  Original versus naive permuted balanced FP32 can return 0 versus 1; the actual
  transported schedule preserves the original word.
- A native opaque-bit butterfly conjugation is a finite exact transformed gate,
  so the coordinatewise automorphism lemma is not a universal cheap-gate no-go.
  Shared bilinear products likewise prevent summing individual output ranks.

Next unresolved construction: a concrete paid `E F_W E^-1` (or a different
global representation) for arbitrary native dense W that avoids its dense
coefficient/work stream. The current wrapper retains that full stream. Do not
rename the butterfly wrapper as the missing producer or rerun its tiny gates
as primary progress. All compiled selectors/decoders and state operations need
actual procedures, exact native semantics, whole costs and sufficient upper
bounds. Existing rejected source reconstruction, witness, row-replay, full-scan,
catalog, per-matrix advice split and sparse-delta premises stay excluded.

THEORY_STATUS=NOT_ESTABLISHED; HARDWARE_STATUS=NOT_TESTED; CORE_ADMISSION=false;
O1-O5 OPEN; O6 PARTIAL. The user reports 405B hardware unavailable. Goal remains
active and unachieved. No target hardware, full HF/KV/RNG or latency run occurred.

The shared original checkout has another active writer. This artifact was built
independently. Before integration, read actual remote/head/status and preserve
that writer's files; do not overwrite or merge unrelated research. The actual
commit/remote SHA must be obtained from Git, not self-asserted in this document.
