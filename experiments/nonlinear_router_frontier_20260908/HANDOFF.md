# Handoff — nonlinear router frontier

Base scientific line before this round: PR #149 head
`866e347540c6aff7595ac81c2d2c6c0cf5680196`.

This branch preregistered three information-flow directions and selected the
global nonlinear bounded-word route. The implementation produced a stronger
than preregistered theorem:

```text
any deterministic t-probe data structure with S arbitrary nonlinear w-bit
checkpoint cells that answers a linear query family exactly implies a cover
by <=S^t coefficient subspaces, each dimension <=t*w.
```

Combining this with exact binary Segre intersection geometry closes the old
`25x108 / 50 words / 2 probes` survivor and all target-feasible side<=128
64-bit local word routers.

First unclosed local shapes by exact area scan:

```text
24x225 and 25x216
D=5400 bits
granted encoded bits=6319
padded words=99
max target probes=4
traffic=8/675 exactly
```

Do not call these constructions. The route-cover upper/required ratios are
5.725598736143037 and 2.862799282753446 respectively, so the current Gate is
simply inconclusive there.

Further refinement closes both nonadaptive four-probe routing and a one-value-
stage adaptive router in which the first returned word chooses all three later
payload addresses. Ratios are:

```text
24x225 nonadaptive        0.22437430765584512
25x216 nonadaptive        0.11218715076256036
24x225 one-value-stage    0.8974972306222921
25x216 one-value-stage    0.44874860277162165
```

Thus any surviving four-probe local constructor must use at least two
successive value-dependent address-routing stages.

The strong independently-selectable 32-query interface closes that remaining
local target route as well. For every exact deterministic 99-word encoding,
some source activates at least 84 distinct words across the complete rank-one
query family. A greedy 32-query tuple therefore touches at least 32 distinct
words, or 2,048 bits. Relative to four favorable Q4 lanes over D=5,400 this is
`64/675`, exactly 8x the registered `8/675` line.

Do not turn that result into a batch-1 Transformer impossibility claim. The
hard tuple is independently selectable in the E0 query service model; ordinary
causal reachability of that tuple is not proved. Physical tier placement and
wall-clock latency are not proved either.

Immediate next scientific work:

1. Build a finite causal-reachability bridge showing whether a legal unchanged
   HF dense model/continuation can realize the hard independent rank-one query
   population, without using reference-future outputs to choose the path; or
   construct a causal-specific/global producer whose finite algorithm provably
   avoids that population while preserving all legal continuations.
2. Do not use a literal row-combination/response catalog, input bank, lookup
   table, linear packed-word dictionary, or full source scan: those are prior
   rejected families.
3. Move to a materially different global cross-matrix/nonlocal producer; the
   present theorem does not establish a global nonlinear direct sum.
4. Native numerical lifting must preserve original accumulation/rounding order,
   complete logits/KV/RNG state and all continuations; no finite-field result is
   a native proof by itself.
5. Keep 405B/CUDA/<=8GiB/native4BQ4/TTFT `NOT TESTED` until actually executed.

Canonical result:

`results/e0_nonlinear_router_cover_gate_v5/summary.json`

SHA-256:

`af113c03b561827fc4ac93b1beb7216eb9de9cc9de17c95e6660b681c66f03d2`

Focused tests 15/15 PASS; related three-suite regression 28/28 PASS. The native
global 3,510-file replay was intentionally not rerun.
