# Recovered interrupted round

This record preserves the bounded work recovered from the unreferenced GitHub
blob `6a009505882dabf861b211d71f22ca46994da91d`. The blob was not present in the
local Git object database at the start of this continuation, but the GitHub
blob API returned its text. It was not a tree or commit and is not treated as
prior durable repository evidence.

Recovered scope:

- base `866e347540c6aff7595ac81c2d2c6c0cf5680196` (PR #149);
- two synthetic BF16 recurrent maps;
- block lengths 2 and 4;
- two initial states;
- 8 cases, 24 complete 97-bit state steps, zero state mismatch;
- 920 native calls versus 24 sequential calls;
- per-case native-call ratios 19x to 50.25x;
- exact matrix-free directional correction simplifies back to sequential `F`;
- the full gate-state-array design failed its storage/cost screen;
- the first nonzero explicit fused-SwiGLU moment tensor failed its
  coefficient/storage screen;
- no HF model, KV, CUDA, 405B, 8 GiB, PCIe/SSD, native-4B-Q4 latency or TTFT
  evidence was generated by that recovered round.

The same blob explicitly preserves the closed error-transport evidence:

- GF(2) correction retains full original dense work;
- naive BF16 additive transport fails finite-word rounding;
- the exact 18-bit relation needs 262,143 alternate evaluations;
- relation consistency does not identify the actual checkpoint.

The later binary blob fragments listed in the session handoff were not decoded
through the UTF-8-only GitHub connector and are not promoted as evidence.
