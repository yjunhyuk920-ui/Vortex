# Next obligation — construct the general native causal source

The fixed mission and CTC-2026-09-05 remain unchanged. [Current scoped construction](experiments/joint_energy_20260907/REPORT.md); [all previous directives, preserved unchanged](docs/research/history/pre_joint_energy_20260907/NEXT_EXPERIMENT.md).

No candidate is promoted. The primary deliverable is still a finite uniform constructor and causal query/state program with original numerical/RNG behavior and a sufficient complete 405B/8-GiB/baseline/TTFT bound, not another catalog of excluded representations.

The current joint source has real generators, reconstruction and exact certificates, but no cheap mechanism giving a small recoverable residual for general native dense inputs. A follow-on must address this missing information source or choose a materially different execution principle. It must not assume <=2 nonzeros, low rank, nonnegative activations, favorable previous inputs or native additivity.

Do not expand the dense Gram, increase the Prony support/order cap, split signed inputs and presume each residual remains sparse, or port the same packet to GPU as a core advance. In the specific positive s<=2 path, strictly positive dense-input success implies rank(W)<=3. Its small conditional payload is not a target speedup and assumes int64 coefficients fit. Real-algebra moments are not automatically moments of rounded outputs.

Any reopening must supply an actual cheap source defeating these premises, including its automatic preparation, address/decoder work, arbitrary-precision costs, supported numerical domain, successor state and finite full budget. No free verifier, perfect sparse-output promise, unbounded table, free fallback or continuation recomputation. Older failure scopes remain method-specific; none is a general impossibility theorem.

Compare distinct principles, then prioritize the strongest surviving construction. Preserve all evidence, validate locally, commit a research branch and read back the actual SHA. No direct main write, merge, force-push or unrequested Actions. A suite or commit is not scientific completion.
