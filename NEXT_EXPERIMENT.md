# Next constructive obligation — 2026-09-08

Verify actual remote head and fixed mission/CTC/O1-O6. [Current frontier](experiments/nonlinear_router_frontier_20260908/REPORT.md).

The new largest-fiber/Segre Gate closes the previous `25x108`, 50-word,
two-probe nonlinear capacity survivor and every target-feasible 64-bit local
word router with both sides <=128. The first local points not rejected by the
full adaptive Gate are `24x225` and `25x216`: 5,400 source bits, 6,319 granted
encoded bits padded to 99 words, four 64-bit probes, exact favorable traffic
`8/675`. They are necessities/capacity survivors, not algorithms.

Nonadaptive four-word decoding is already rejected on both. So is a one-value-
stage design where the first returned word chooses all three later payload
addresses. The remaining multi-stage route also fails the stronger
independently-selectable 32-query interface: source-identification forces some
source to activate at least 84 of 99 words over the complete rank-one family,
and a greedy 32-query tuple touches at least 32 distinct words = 2,048 bits =
`64/675`, exactly 8x the registered `8/675` line.

The next **constructive** obligation is therefore not another local router
tuning. Compare three materially different causal/global principles first and
then solve the strongest finite missing construction. At minimum include:

1. a legal-causal reachability compiler that decides whether an unchanged HF
   dense checkpoint plus legal prefix/input sequence can realize the abstract
   hard rank-one query tuple without reference-future leakage;
2. a globally mixed nonlinear checkpoint producer whose encoder, address rule,
   decoder and 32-token physical union are explicit rather than a local direct
   sum; and
3. a paid dynamic causal-state information source whose update/query rule
   creates future dense effects rather than merely serializing/replaying KV.

The hard independent tuple is not yet a batch-1 Transformer reachability proof,
so do not declare general impossibility from it. Conversely, a causal-specific
survivor must provide a real finite producer; merely saying real causal queries
are a smaller set is not a construction.

Do not replace this obligation with a literal response/row-combination catalog,
input bank, lookup table, packed linear dictionary, static circuit, full source
scan, or related-input correction. Those are existing rejected families. If
both local four-probe points close, immediately start another genuinely
different three-principle round aimed at global cross-matrix/nonlocal
checkpoint information; do not assume a direct-sum split of the 8 GiB advice.

Only after a finite binary/numerical source survives may it be lifted to native
Q4/BF16/FP32 accumulation, full logits/KV/RNG successor state, all legal HF
continuations, and a paid target-scale schedule. The native-global A/C/B record
still preserves observed state but retains dominant dense work; do not rerun it
as primary discovery.

## Prior exclusions and preserved frontier

All432frozen packets on original SmolLM2 weights and synthetic inputs have distinct
BF16 outputs. Tightening one common enclosure cannot make them broadcastable.
Do not tune packets,seeds,selected tensors or planted dominance as next core work.

Construct a paid HETEROGENEOUS native effect producer or a different exact whole
body/state computation. A shared pilot plus unspecified cheap correction merely
renames the missing work. Delayed action must charge later forcing/observer state,
preserving PR145 evidence. No viable native producer is supplied by the current
binary Gate.

The prior packet screen's real weights are not causal activation evidence. Current
A/B/C separately establish only their actual pinned-CPU frozen HF protocol;
declared balancedFP32 or a different platform is not automatically this ABI.
Three qualifying principles and O1-O6 remain absent; fixed mission continues.
Preserve old failure scopes. PR147 correlation-source and local mantissa-source
remain separate; incorporate [actual concurrent frontier](experiments/output_envelope_20260908/FRONTIER_SYNC.md).
[Previous next unchanged](docs/research/history/pre_output_envelope_20260908/NEXT_EXPERIMENT.md).
[Pre-native-global next](docs/research/history/pre_native_global_transition_20260908/NEXT_EXPERIMENT.md).
