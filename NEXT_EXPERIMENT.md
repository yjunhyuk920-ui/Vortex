# Next constructive obligation — 2026-09-08

Verify actual remote head and fixed mission/CTC/O1-O6. [Current frontier](experiments/causal_global_bridge_20260908/REPORT.md).

The causal/global three-principle round is now executed. A real HF causal
exposure compiler and a restricted exact basis-column producer exist; a
standard-causal sign/delta compiler carries many independently chosen right
factors. Principle B still lacks an arbitrary-checkpoint global
encoder/address/decoder, while literal Principle-C response-column updating is
closed by legal densely changing right factors.

The next **constructive** obligation is the missing object itself, not another
name for it:

```text
G = Compile(checkpoint_all)                          finite, fully paid
addresses = Address(G, current_input, exact_state)  finite causal algorithm
words = Read(G, addresses)                          paid physical payload
(effect,state') = Decode(words,input,state)         native finite-word exact
```

It must work for arbitrary unchanged checkpoint matrices, allow globally mixed
advice without an assumed per-matrix direct sum, and produce the exact ordered
Q4/BF16/FP32 dense effect and required successor state while closing the 8 GiB
and native-4B-Q4 traffic/arithmetic line. Boolean-semiring randomized MatVec,
bitset full scans, response catalogs and undefined compression/decoders are not
this object.

For a dynamic alternative, use the new sign/delta causal compiler as the
adversary: successive legal right factors may differ in every coordinate. Any
update law that explicitly adds the changed source columns is full dense work
and is already closed. A valid dynamic summary must aggregate such arbitrary
dense changes subdensely with a finite native-exact update rule.

The area-5,400 `25x216` 32-query native trace remains a useful bounded bridge
check, but running it is not a substitute for constructing the global producer.
Current native evidence is four queries at that exact source shape plus a full
32-query/exhaustive-row control at width 8. Do not interpolate those facts into
a 405B impossibility or target-hardware result.

Do not replace this obligation with a literal response/row-combination catalog,
input bank, lookup table, packed linear dictionary, static circuit, full source
scan, or related-input correction. Those are existing rejected families. The
active direction is already global cross-matrix/nonlocal checkpoint information;
do not regress to local four-probe tuning or assume a direct-sum split of the
8 GiB advice.

The restricted binary producer has survived only its declared E1 family. Its
next useful generalization must expand the source alphabet/right-factor family
without returning to full matrix reads. The native-global A/C/B record still
preserves observed state but retains dominant dense work; do not rerun it as
primary discovery.

## Prior exclusions and preserved frontier

All432frozen packets on original SmolLM2 weights and synthetic inputs have distinct
BF16 outputs. Tightening one common enclosure cannot make them broadcastable.
Do not tune packets,seeds,selected tensors or planted dominance as next core work.

Construct a paid HETEROGENEOUS native effect producer or a different exact whole
body/state computation. A shared pilot plus unspecified cheap correction merely
renames the missing work. Delayed action must charge later forcing/observer state,
preserving PR145 evidence. The new binary basis-column producer is viable only
inside its explicitly restricted family and does not supply the arbitrary-native
producer required here.

The prior packet screen's real weights are not causal activation evidence. Current
A/B/C separately establish only their actual pinned-CPU frozen HF protocol;
declared balancedFP32 or a different platform is not automatically this ABI.
Three qualifying principles and O1-O6 remain absent; fixed mission continues.
Preserve old failure scopes. PR147 correlation-source and local mantissa-source
remain separate; incorporate [actual concurrent frontier](experiments/output_envelope_20260908/FRONTIER_SYNC.md).
[Previous next unchanged](docs/research/history/pre_output_envelope_20260908/NEXT_EXPERIMENT.md).
[Pre-native-global next](docs/research/history/pre_native_global_transition_20260908/NEXT_EXPERIMENT.md).
