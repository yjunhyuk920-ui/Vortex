# Bound Query-Adaptive Exact Code Unions

Type: research
Status: claimed
Blocked by: Run the Causal Bilinear Rank Gate

## Question

Can a causal router select one of multiple separately built exact Bilinear
Cross Residual codebooks so that each query reads and scans only one small
codebook, thereby escaping EXP-084A's single full-factor span, while the
complete registered 405B construction, representation, selector, query
operations, logical/address traffic, hot/cold state, verification, miss, and
fallback equations remain below the Fixed Mission Gates?

First analyze the strongest favorable **query-adaptive union of exact linear
codes**: each leaf contains at most the registered scannable factor dimension,
the selector and leaf lookup may initially be granted zero cost only as a
necessary ceiling, every leaf's build and persistent state are charged, and a
hit still requires an exact rational membership witness. Derive a finite
capacity/coverage bound and use only the already frozen EXP-084A factor arrays
for any zero-forward diagnostic.

Do not repartition or retune the EXP-084A population and call it new evidence;
do not run another checkpoint row, assign EXP-085, contact the Ubuntu server,
download a model, train a router, or build a backend. If the union survives
E0, specify the concrete causal selector and cheapest untouched Gate before
E1. If it reduces to subsets of one calibration span, requires an uncharged
membership oracle, or fails the complete equation, reject that declared class
without a parameter sweep.

