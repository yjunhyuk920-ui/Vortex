# Additive scientific ledger — NTC-2026-09-05

This round supplements, rather than edits or erases, the existing append-only
root decision, failure, assumption, architecture and reproduction records.
Current root state/README/validation/next-work link here. Earlier decisions retain
their exact scope. The full construction and proof is in
[NATIVE_TRANSFER_CONSTRUCTION.md](NATIVE_TRANSFER_CONSTRUCTION.md).

## Decision NT-D1

Retain guarded phase composition, causal suffix reference and exact rounding-cell
inverse as scoped E1 numerical artifacts. No high-speed core, full theory or
hardware promotion. Ordinary synthetic suffix certification failed 36/36 rows;
no post-hoc threshold/width sweep is authorized. Continue only on the missing
paid information source/native-state lift, not another isolated size increase.

## Assumption register additions

| ID | Assumption | Actual status |
|---|---|---|
| NT-A1 | exact native summaries can be cheaply built/reused for arbitrary queries | NOT ESTABLISHED; constructor reads every product |
| NT-A2 | short suffixes erase typical incoming-prefix uncertainty | CONTRADICTED on frozen ordinary synthetic screen; real checkpoints NOT TESTED |
| NT-A3 | exact inverse computation provides a cheap correct output proposal | NOT ESTABLISHED; known test output is an oracle control only |
| NT-A4 | serial fmaf equals every target's original reduction | NOT ESTABLISHED; original reference ABI not pinned |
| NT-A5 | all static prefix metadata fits hot 8 GiB | false for illustrated uniform 2-byte/32-coefficient model-wide layout; cold costs required |
| NT-A6 | missing GPU is the only obstacle | false; core algorithm and full O1-O6 remain open |

## Anti-repetition addition NT-F1

Do not promote the recorded suffix algorithm by quoting its contrived positive
control, omitting the activation scan/index build/retries, or calling distinct
logical operand reads physical SSD/GPU savings. Do not remove the full-prefix
binade guard, call two phases two bits, assume free query-dependent descriptor
construction, or treat the inverse's known test output as a causal selector.
F-065/F-066 and prior nonnative/semiring failures are unchanged. No universal
impossibility is proved by this synthetic screen.

## Architecture addition NT-R1

The added reference modules live only in `experiments/native_transfer/` and
`tests/native_transfer/`. No existing `vortex_runtime` operator is replaced.
Possible substitution is conditional on the actual original native graph and
identical boundary state/RNG behavior. Full-module lifting and a paid memory
scheduler are not implemented. The old runtime architecture is not certified
by these scalar references.

## Reproduction addition NT-P1

Use the exact commands in the current README. Initial/continuation preregistration,
environment, test failure/pass logs, raw summary/rows, inverse recording hashes,
integer-only evidence decoder, regeneration and checksums are retained. The two
large raw captures are losslessly predictor/residual encoded for remote storage;
they are not merely guessed from a summary. Decoding was compared byte-for-byte
to the earlier native captures. This is archival encoding, not another native
measurement or a compression result for model weights. The downloadable bundle
also contains literal raw files.

Previous four root snapshots are copied using their original Git blob SHAs;
the pinned base and all prior executable files remain in the research ancestry.
Remote commit verification is a separate handoff fact and never scientific
promotion. No Actions, unrelated branch merge, target-machine access or public
checkpoint download was performed.
