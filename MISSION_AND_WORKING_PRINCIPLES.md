# VORTEX 목표와 작업 원칙

Version: CTC-2026-09-05. “우리의 목표와 작업 원칙은?”에 대한 정본.

## 고정 목표

임의의 공개·미수정 Hugging Face Dense 405B급 모델을 재학습·미세조정·증류·가중치 수정 없이 실행기만 교체하여, 단일 8 GiB GPU에서 원본 출력/RNG와 필요한 다음 상태를 보존하면서 같은 머신의 native 4B Q4에 가까운 지연으로 실행한다. 배치 크기는 1이며 warm time/token 목표는 p50 <=1.2x, p95 <=1.5x이다. 기존 TTFT 요구도 유지한다.

특정 모델·프롬프트의 우연, 숨은 원격 연산, 무제한 전처리, 무료 검증·복구·fallback으로 목표를 바꾸지 않는다. CPU RAM, SSD, PCIe, HBM, 전체 GPU 할당, KV/cache, 메타데이터와 생성·이동·계산·검증·복구 비용을 전부 계상한다.

## 가장 중요한 원칙

**검증하기 쉬운 작은 문제를 계속 찾는 것이 아니라, 고정 목표를 충족하는 완결된 실행 방법을 구성한다.**

핵심 산출물은 구체적인 자동 생성 절차와 실행 알고리즘, 원본 동작 보존 증명, 전체 자원·시간 상한, 목표 규모 예산 충족 증명이 연결된 구성적 이론이다. 작은 필요조건, 일부 연산의 정확성, 후보 탈락, 시험 통과나 커밋만으로 이를 대신하지 않는다.

[구성적 이론 계약](docs/CONSTRUCTIVE_THEORY_CONTRACT.md)이 이론 달성, 연구 우선순위, 구조 중립성, 종료 판정의 세부 기준이다. 이 범위에서 충돌하는 이전 문구보다 우선하되, 고정 목표나 실제 실험 증거를 바꾸지 않는다.

## 연구 수행 원칙

1. **최종 정리에서 역산한다.** 적용 범위, 자동 구성, 인과적 실행, 원본 수치/RNG·상태 보존, 전체 비용, 목표 예산, 재검증을 증명 의무 O1-O6으로 나눈다. 각 주 작업은 어떤 핵심 빈칸을 해결하는지 명시한다.
2. **서로 다른 새 실행 원리 3개를 먼저 비교한다.** 숨은 전제·계산 순서·정보 흐름·검증 단위 중 하나 이상을 뒤집고, 각 원리에 정확성 식, 전 비용 식, 명시적인 10배 이상 제거·상각 경로, 가장 강한 반례와 가장 싼 결정적 검증을 붙인다. 기존 기법의 조합·미세변형이나 이름만 다른 후보로 채우지 않는다. 가장 강한 생존 후보의 미해결 구성을 끝까지 좁힌다.
3. **하한 통과와 상한 증명을 구분한다.** “최소 필요 비용이 목표보다 작다”는 성공 증명이 아니다. 실제 알고리즘의 유한 실행 순서와 충분한 상한으로 목표를 닫는다. 10배 절감과 4/405 비율은 최종 지연 통과 판정이 아니다.
4. **핵심 난제를 가정으로 숨기지 않는다.** 선택·인증·복원·디코딩·상태 갱신을 미정의 함수, 완벽한 선택기, 미증명 압축·재사용률, 무한 탐색으로 대체하지 않는다. 기계모형의 전제와 반드시 증명할 알고리즘 성질을 구분한다.
5. **작은 실험을 실제 해법에 연결한다.** 어떤 Transformer 연산과 증명 의무를 다루며 원본 수치·다음 상태·405B 규모로 어떻게 이어지는지 먼저 적는다. 연결 없는 작은 반례·증명은 제한된 보조 작업이다. 실패한 계열은 새 정보원·계산 의존관계·규모 법칙·실측 근거 없이 반복하지 않는다.
6. **목표만 고정하고 실행 구조는 열어 둔다.** 긴 accepted block이나 특정 A, N/A, r 조건을 전혀 다른 원리에도 강요하지 않는다. 선택한 구조 자체의 정확성과 전체 비용을 증명한다.
7. **후보 종료·세션 종료·목표 달성을 분리한다.** 후보 하나의 실패나 작은 증명 완료는 자동 연구 종료가 아니다. 세션 안에서 가능한 다음 핵심 구성으로 진행한다. 경계에 도달하면 미해결 의무를 정확히 인계하되 실패를 감추거나 성공을 꾸미지 않는다.

기본 우선순위는 큰 개선이 가능한 구성적 연구 70%, 저비용 결정적 반증 20%, 보조 구현·정리 10%다. 측정하지 않은 작업 시간을 이 비율로 꾸미지 않는다.

## 이론 달성과 실제 달성

`THEORY_STATUS=VERIFIED_IN_MODEL`은 모든 핵심 의무가 닫히고, 구체적인 유한 알고리즘과 재검증 가능한 증명으로 명시된 기계모형에서 목표 예산을 충족한 상태다. 중요한 알고리즘 가정이 남으면 CONDITIONAL이며 완성으로 부르지 않는다.

`HARDWARE_STATUS=E7_VERIFIED`는 실제 405B와 대상 머신에서 원본 계약, 전체 8 GiB 적합성, 같은 머신 4B Q4 대비 지연 및 재현 기준을 통과한 상태다. 모형 내 증명을 실측 달성으로 바꾸어 말하지 않는다.

`HANDOFF_STATUS=REMOTE_COMMIT_VERIFIED`는 결과가 실제 원격 저장소에 있다는 뜻일 뿐이다. 커밋으로 이론이나 목표가 달성되지는 않는다.

## 로컬 검증과 원격 기록

```text
LOCAL_RESEARCH -> LOCAL_VALIDATION_PASS -> COMMIT_PUSHED -> REMOTE_COMMIT_VERIFIED
GITHUB_ACTIONS_REQUIRED=false
GITHUB_REEXECUTION_REQUIRED=false
```

연구와 가능한 검증을 로컬/샌드박스에서 수행한다. 가설·입력·기준은 결과를 보기 전에 로컬에서 고정하고 증거와 함께 보존한다. GitHub는 커밋·인계에 사용하며 Actions는 명시적으로 요청된 경우에만 사용한다. 브랜치 보호는 우회하지 않는다.

채팅·ZIP·로컬 커밋만 남기지 않는다. 연구 브랜치에 실제 커밋하고 원격 SHA와 변경 파일을 재조회한다. main에 직접 push하거나 강제 push하지 않고, 기존 미병합 연구를 임의로 섞지 않는다. 모든 필요한 원시 결과·로그·체크섬·판정·미해결 가정과 인계를 보존한다.

README는 매 의미 있는 회차에 확인하고 변경된 원칙·상태·실행법을 같은 커밋에 반영한다. 문서 변경 회차는 문서·링크·계약 일관성을 검증하며 실행하지 않은 런타임 시험을 주장하지 않는다. 실측/유도/예측/미검증을 구분하고 실제 못 한 항목은 NOT TESTED로 남긴다.

보고의 중심은 “무슨 실행 방법을 만들고 어떤 핵심 의무를 해결했는가”다. 시험 개수와 커밋은 뒤의 재현 근거다. 미완성이면 결정적인 남은 구성을 짧고 명확하게 밝힌다.
