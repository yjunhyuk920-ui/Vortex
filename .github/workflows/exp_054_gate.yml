name: EXP-054 reduced decision-diagram Gate

on:
  workflow_dispatch:

concurrency:
  group: exp-054-manual-${{ github.ref }}
  cancel-in-progress: true

jobs:
  reproduce:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install pinned validation environment
        run: |
          set -euo pipefail
          python -m pip install --disable-pip-version-check pytest==8.3.5 numpy==2.1.3
          python -m pip install --disable-pip-version-check --index-url https://download.pytorch.org/whl/cpu torch==2.6.0
          python -m pip install --disable-pip-version-check transformers==4.50.3 huggingface_hub==0.29.3 safetensors==0.5.3 tokenizers==0.21.1
      - name: Run isolated reproduction
        env:
          PYTHONPATH: ${{ github.workspace }}
          EXP054_OUTPUT_DIR: ${{ runner.temp }}/exp_054_reproduction
        run: bash experiments/exp_054/reproduce.sh
      - name: Upload isolated reproduction
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: exp-054-manual-reproduction-${{ github.run_id }}
          path: ${{ runner.temp }}/exp_054_reproduction
          if-no-files-found: warn
          retention-days: 30
