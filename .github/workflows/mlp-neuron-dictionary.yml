name: MLP neuron dictionary gate

on:
  pull_request:
    paths:
      - "vortex_runtime/mlp_neuron_dictionary.py"
      - "scripts/run_mlp_neuron_dictionary_point.py"
      - "tests/test_mlp_neuron_dictionary.py"
      - "docs/EXPERIMENT_017_MLP_NEURON_DICTIONARY.md"
      - ".github/workflows/mlp-neuron-dictionary.yml"

concurrency:
  group: mlp-neuron-dictionary-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  contents: write
  pull-requests: write

jobs:
  measure:
    if: github.head_ref == 'research/mlp-neuron-dictionary'
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        include:
          - tiny_prototypes: 16
            target_prototypes: 128
          - tiny_prototypes: 32
            target_prototypes: 256
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install CPU runtime
        run: |
          python -m pip install --upgrade pip
          pip install torch --index-url https://download.pytorch.org/whl/cpu
          pip install -e .
          pip install pytest transformers sentencepiece
      - name: Validate neuron dictionary primitives
        run: python -m pytest -q tests/test_mlp_neuron_dictionary.py
      - name: Measure ${{ matrix.tiny_prototypes }} prototypes per layer
        run: |
          python scripts/run_mlp_neuron_dictionary_point.py \
            --model TinyLlama/TinyLlama-1.1B-Chat-v1.0 \
            --device cpu \
            --tiny-prototypes ${{ matrix.tiny_prototypes }} \
            --target-prototypes ${{ matrix.target_prototypes }} \
            --projection-dim 64 \
            --cluster-iterations 5 \
            --factor-bits 8 \
            --seed 4049 \
            --tokens 32 \
            --tree-depth 12 \
            --top-k 32 \
            --beam-width 64 \
            --node-budget 1024 \
            --eval-batch-size 8 \
            --eval-prompt "한국어로 정렬 알고리즘의 시간 복잡도를 설명해줘." \
            --output "mlp-dictionary-${{ matrix.tiny_prototypes }}.json"
      - uses: actions/upload-artifact@v4
        with:
          name: mlp-dictionary-${{ matrix.tiny_prototypes }}
          path: mlp-dictionary-${{ matrix.tiny_prototypes }}.json

  aggregate:
    needs: measure
    if: needs.measure.result == 'success'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
      - uses: actions/download-artifact@v4
        with:
          pattern: mlp-dictionary-*
          path: mlp-dictionary-results
          merge-multiple: true
      - name: Aggregate MLP dictionary frontier
        env:
          RUN_ID: ${{ github.run_id }}
          HEAD_SHA: ${{ github.sha }}
        run: |
          python - <<'PY'
          import hashlib
          import json
          import os
          from pathlib import Path

          files = sorted(Path("mlp-dictionary-results").glob("*.json"))
          if not files:
              raise SystemExit("no MLP dictionary results")
          points = [json.loads(path.read_text(encoding="utf-8")) for path in files]
          points.sort(key=lambda item: item["tiny_prototypes_per_layer"])
          best = max(
              points,
              key=lambda item: (
                  item["tree"]["exact_path_last_alive_depth"],
                  item["teacher_forced_compiled"]["top32_rate"],
                  item["teacher_forced_compiled"]["top1_rate"],
              ),
          )
          promoted = [point for point in points if point["qualifies"]]
          summary = [
              {
                  "tiny_prototypes": point["tiny_prototypes_per_layer"],
                  "target_prototypes": point[
                      "projected_405b_prototypes_per_layer"
                  ],
                  "factor_gib": point["405b_partial_mlp_budget"]["factor_gib"],
                  "remaining_traffic_gib": point[
                      "405b_partial_mlp_budget"
                  ]["remaining_traffic_gib_per_token"],
                  "gate_up_error": point["fit"][
                      "neuron_weighted_gate_up_relative_l2_error"
                  ],
                  "teacher_top1": point["teacher_forced_compiled"]["top1_rate"],
                  "teacher_top32": point["teacher_forced_compiled"]["top32_rate"],
                  "autoregressive_prefix": point[
                      "autoregressive_exact_prefix"
                  ],
                  "tree_prefix": point["tree"][
                      "optimistic_committed_exact_prefix"
                  ],
                  "last_alive_depth": point["tree"][
                      "exact_path_last_alive_depth"
                  ],
                  "promoted": point["qualifies"],
              }
              for point in points
          ]
          if promoted:
              decision = "advance MLP neuron dictionary to complete architecture"
              next_candidate = (
                  "compile attention-head dictionaries and an LM-head candidate "
                  "program inside the remaining traffic headroom"
              )
          elif best["teacher_forced_compiled"]["top32_rate"] >= 0.5:
              decision = "retain neuron dictionary and add scale bins or residual neurons"
              next_candidate = (
                  "allocate remaining MLP bytes to multiple gate/up scale bins "
                  "without increasing executed traffic above the partial Gate"
              )
          else:
              decision = "reject tested MLP neuron-collapse frontier"
              next_candidate = (
                  "test exact head/MLP permutation alignment followed by sparse "
                  "prototype mixtures rather than hard assignments"
              )
          payload = {
              "evidence_level": "E2 executable MLP neuron dictionary frontier",
              "points": points,
              "summary": summary,
              "best_tiny_prototypes": best["tiny_prototypes_per_layer"],
              "promoted_points": [
                  point["tiny_prototypes_per_layer"] for point in promoted
              ],
              "decision": decision,
              "next_candidate": next_candidate,
              "workflow": {
                  "run_id": int(os.environ["RUN_ID"]),
                  "head_sha": os.environ["HEAD_SHA"],
                  "input_sha256": {
                      path.name: hashlib.sha256(path.read_bytes()).hexdigest()
                      for path in files
                  },
              },
          }
          output = Path("results/tinyllama_1_1b_mlp_neuron_dictionary.json")
          output.parent.mkdir(parents=True, exist_ok=True)
          output.write_text(
              json.dumps(payload, indent=2, ensure_ascii=False, allow_nan=False),
              encoding="utf-8",
          )
          Path("mlp-dictionary-comment.md").write_text(
              "## MLP neuron dictionary gate\n\n```json\n"
              + json.dumps(
                  {
                      "summary": summary,
                      "best_tiny_prototypes": payload[
                          "best_tiny_prototypes"
                      ],
                      "promoted_points": payload["promoted_points"],
                      "decision": payload["decision"],
                      "next_candidate": payload["next_candidate"],
                  },
                  indent=2,
                  ensure_ascii=False,
              )
              + "\n```",
              encoding="utf-8",
          )
          PY
      - name: Publish report
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: gh pr comment "$PR_NUMBER" --body-file mlp-dictionary-comment.md
      - uses: actions/upload-artifact@v4
        with:
          name: tinyllama-mlp-neuron-dictionary
          path: results/tinyllama_1_1b_mlp_neuron_dictionary.json
      - name: Commit evidence
        env:
          BRANCH: ${{ github.head_ref }}
        run: |
          path=results/tinyllama_1_1b_mlp_neuron_dictionary.json
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "$path"
          git commit -m "data(frontier): commit MLP neuron dictionary sweep"
          git pull --rebase origin "${BRANCH}"
          git push origin "HEAD:${BRANCH}"
